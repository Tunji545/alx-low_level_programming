#include "main.h"
#include "stdlib.h"
/**
*  *_abs - computes the absolute
*
*   *value of an integer
*
*    *@n: integer
*
*     *Return: 0
*/
int _abs(int n)
{
	int x;

	if(n < 0)
	{
		n = -n;
		x = abs(n)
		_putchar(x);
		_putchar('\n');
		return (0);
	}
	x = abs(n)
	_putchar(x);
	_putchar('\n');
	return (0);
}
