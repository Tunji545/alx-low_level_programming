#include "main.h"
#include <ctype.h>
/**
*  *_isalpha - checks for alphabetic characters
*
*   *@c: integer
*
*    *Return: 1
*/
int _isalpha(int c)
{
	if (isalpha(c)
	{
		return (1);
	}
	return (0);
}
