#include <stdio.h>
/**
 *  *main -> assign a random number
 *
 *   *and print the last digit of the
 *
 *    *Return: Always 0 (Success)
 *
 */
int main(void)

{
	int ch;
		for (ch = 97; ch <= 122; ch++)
	{
		putchar(ch);
	}
	for (ch = 65; ch <= 90; ch++)
	{
		putchar(ch);
	}
	putchar(10);
	return (0);
}


